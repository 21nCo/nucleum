/**
 * Auto-generated Drizzle schema for authfn
 * Generated by Superfunctions CLI
 * 
 * DO NOT EDIT MANUALLY
 * 
 * To regenerate: npx superfunctions generate-schema
 */

import { boolean, index, integer, json, pgTable, text, timestamp, uniqueIndex } from 'drizzle-orm/pg-core';

export const users = pgTable(
  'nucleus_account_users',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  emailVerifiedAt: timestamp('email_verified_at', { withTimezone: true }),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  primaryEmail: text('primary_email'),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_authfn_users_primary_email: uniqueIndex('idx_authfn_users_primary_email').on(table.primaryEmail)
  })
);


export const sessions = pgTable(
  'nucleus_account_sessions',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  csrfHash: text('csrf_hash'),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  lastAuthenticatedAt: timestamp('last_authenticated_at', { withTimezone: true }),
  metadata: json('metadata'),
  methods: json('methods').notNull(),
  revokedAt: timestamp('revoked_at', { withTimezone: true }),
  tokenHash: text('token_hash').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull().references(() => users.id),
  },
  (table) => ({
    idx_authfn_sessions_expires_at: index('idx_authfn_sessions_expires_at').on(table.expiresAt),
    idx_authfn_sessions_token_hash: uniqueIndex('idx_authfn_sessions_token_hash').on(table.tokenHash),
    idx_authfn_sessions_user_id_created_at: index('idx_authfn_sessions_user_id_created_at').on(table.userId, table.createdAt)
  })
);


export const password_credentials = pgTable(
  'nucleus_account_password_credentials',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  passwordHash: text('password_hash').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_authfn_password_credentials_user_id: uniqueIndex('idx_authfn_password_credentials_user_id').on(table.userId)
  })
);


export const otp_challenges = pgTable(
  'nucleus_account_otp_challenges',
  {
  attemptCount: integer('attempt_count').notNull(),
  codeHash: text('code_hash').notNull(),
  consumedAt: timestamp('consumed_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  deliveryMetadata: json('delivery_metadata'),
  email: text('email').notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  purpose: text('purpose').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_authfn_otp_challenges_email_purpose_created_at: index('idx_authfn_otp_challenges_email_purpose_created_at').on(table.email, table.purpose, table.createdAt),
    idx_authfn_otp_challenges_expires_at: index('idx_authfn_otp_challenges_expires_at').on(table.expiresAt)
  })
);


export const oauth_states = pgTable(
  'nucleus_account_oauth_states',
  {
  code_verifier: text('code_verifier'),
  consumed_at: text('consumed_at'),
  created_at: text('created_at').notNull(),
  expires_at: text('expires_at').notNull(),
  nonce: text('nonce'),
  provider_id: text('provider_id').notNull(),
  redirect_uri: text('redirect_uri').notNull(),
  requested_scopes: json('requested_scopes').notNull(),
  state_id: text('state_id').notNull().unique(),
  subject_key: text('subject_key').notNull(),
  subject_kind: text('subject_kind').notNull(),
  subject_payload: json('subject_payload').notNull(),
  },
  (table) => ({
    idx_oauth_states_expires_at: index('idx_oauth_states_expires_at').on(table.expires_at)
  })
);


export const oauth_tokens = pgTable(
  'nucleus_account_oauth_tokens',
  {
  connection_id: text('connection_id').notNull().unique(),
  created_at: text('created_at').notNull(),
  encrypted_payload: text('encrypted_payload').notNull(),
  expires_at: text('expires_at'),
  key_ref: text('key_ref').notNull(),
  provider_id: text('provider_id').notNull(),
  tenant_id: text('tenant_id').notNull(),
  token_id: text('token_id').notNull().unique(),
  updated_at: text('updated_at').notNull(),
  user_id: text('user_id').notNull(),
  },
  (table) => ({
    idx_oauth_tokens_connection: index('idx_oauth_tokens_connection').on(table.connection_id)
  })
);


export const oauth_consents = pgTable(
  'nucleus_account_oauth_consents',
  {
  consent_id: text('consent_id').notNull().unique(),
  granted_at: text('granted_at').notNull(),
  metadata: json('metadata'),
  provider_id: text('provider_id').notNull(),
  scope_set: json('scope_set').notNull(),
  subject_key: text('subject_key').notNull(),
  subject_kind: text('subject_kind').notNull(),
  subject_payload: json('subject_payload').notNull(),
  updated_at: text('updated_at').notNull(),
  },
  (table) => ({
    idx_oauth_consents_provider_subject: index('idx_oauth_consents_provider_subject').on(table.provider_id, table.subject_key)
  })
);


export const oauth_revocation_failures = pgTable(
  'nucleus_account_oauth_revocation_failures',
  {
  error_code: text('error_code').notNull(),
  error_message: text('error_message').notNull(),
  failure_id: text('failure_id').notNull().unique(),
  metadata: json('metadata'),
  occurred_at: text('occurred_at').notNull(),
  provider_id: text('provider_id').notNull(),
  retryable: boolean('retryable').notNull(),
  subject_key: text('subject_key').notNull(),
  subject_kind: text('subject_kind').notNull(),
  subject_payload: json('subject_payload').notNull(),
  token_id: text('token_id'),
  token_type_hint: text('token_type_hint'),
  },
  (table) => ({
    idx_oauth_revocation_failures_provider_subject: index('idx_oauth_revocation_failures_provider_subject').on(table.provider_id, table.subject_key)
  })
);


export const oauth_accounts = pgTable(
  'nucleus_account_oauth_accounts',
  {
  connectionId: text('connection_id').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  email: text('email'),
  id: text('id').primaryKey().notNull(),
  profile: json('profile'),
  provider: text('provider').notNull(),
  providerAccountId: text('provider_account_id').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull().references(() => users.id),
  },
  (table) => ({
    idx_authfn_oauth_accounts_connection_id: uniqueIndex('idx_authfn_oauth_accounts_connection_id').on(table.connectionId),
    idx_authfn_oauth_accounts_provider_account: uniqueIndex('idx_authfn_oauth_accounts_provider_account').on(table.provider, table.providerAccountId),
    idx_authfn_oauth_accounts_user_id: index('idx_authfn_oauth_accounts_user_id').on(table.userId)
  })
);


export const two_factor_enrollments = pgTable(
  'nucleus_account_two_factor_enrollments',
  {
  confirmedAt: timestamp('confirmed_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  lastUsedCounter: integer('last_used_counter'),
  secretEncrypted: text('secret_encrypted').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_authfn_two_factor_enrollments_user_id: uniqueIndex('idx_authfn_two_factor_enrollments_user_id').on(table.userId)
  })
);


export const two_factor_recovery_codes = pgTable(
  'nucleus_account_two_factor_recovery_codes',
  {
  codeHash: text('code_hash').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  enrollmentId: text('enrollment_id').notNull(),
  id: text('id').primaryKey().notNull(),
  usedAt: timestamp('used_at', { withTimezone: true }),
  },
  (table) => ({
    idx_authfn_two_factor_recovery_codes_code_hash: uniqueIndex('idx_authfn_two_factor_recovery_codes_code_hash').on(table.codeHash),
    idx_authfn_two_factor_recovery_codes_enrollment_id: index('idx_authfn_two_factor_recovery_codes_enrollment_id').on(table.enrollmentId)
  })
);


export const two_factor_challenges = pgTable(
  'nucleus_account_two_factor_challenges',
  {
  consumedAt: timestamp('consumed_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  primaryMethod: text('primary_method').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_authfn_two_factor_challenges_expires_at: index('idx_authfn_two_factor_challenges_expires_at').on(table.expiresAt),
    idx_authfn_two_factor_challenges_user_id: index('idx_authfn_two_factor_challenges_user_id').on(table.userId)
  })
);


export const api_keys = pgTable(
  'nucleus_account_api_keys',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }),
  id: text('id').primaryKey().notNull(),
  lastUsedAt: timestamp('last_used_at', { withTimezone: true }),
  metadata: json('metadata'),
  name: text('name'),
  revokedAt: timestamp('revoked_at', { withTimezone: true }),
  scopes: json('scopes'),
  secretHash: text('secret_hash').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id'),
  },
  (table) => ({
    idx_authfn_api_keys_secret_hash: uniqueIndex('idx_authfn_api_keys_secret_hash').on(table.secretHash),
    idx_authfn_api_keys_user_id_created_at: index('idx_authfn_api_keys_user_id_created_at').on(table.userId, table.createdAt)
  })
);


export const region_profiles = pgTable(
  'nucleus_account_region_profiles',
  {
  authority: text('authority').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  domain: text('domain'),
  id: text('id').primaryKey().notNull(),
  regionId: text('region_id').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_authfn_region_profiles_region_id: index('idx_authfn_region_profiles_region_id').on(table.regionId),
    idx_authfn_region_profiles_user_id: uniqueIndex('idx_authfn_region_profiles_user_id').on(table.userId)
  })
);


export const native_handoff_codes = pgTable(
  'nucleus_account_native_handoff_codes',
  {
  codeHash: text('code_hash').notNull(),
  consumedAt: timestamp('consumed_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  regionId: text('region_id').notNull(),
  sourceSessionId: text('source_session_id').notNull(),
  target: text('target').notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_authfn_native_handoff_code_hash: uniqueIndex('idx_authfn_native_handoff_code_hash').on(table.codeHash),
    idx_authfn_native_handoff_expires_at: index('idx_authfn_native_handoff_expires_at').on(table.expiresAt),
    idx_authfn_native_handoff_source_session_id: index('idx_authfn_native_handoff_source_session_id').on(table.sourceSessionId)
  })
);


export const widget_tokens = pgTable(
  'nucleus_account_widget_tokens',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  regionId: text('region_id').notNull(),
  revokedAt: timestamp('revoked_at', { withTimezone: true }),
  scopes: json('scopes').notNull(),
  tokenHash: text('token_hash').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_nucleus_widget_tokens_hash: uniqueIndex('idx_nucleus_widget_tokens_hash').on(table.tokenHash),
    idx_nucleus_widget_tokens_user: index('idx_nucleus_widget_tokens_user').on(table.userId)
  })
);