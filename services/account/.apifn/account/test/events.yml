info:
  name: Test Events
  type: http
  seq: 15
http:
  method: GET
  url: "{{baseUrl}}/__test/events"
runtime:
  scripts:
    - type: tests
      code: |
        test("auth events were captured", function() {
          expect(res.status).to.equal(200);
          expect(res.body.events.length).to.be.above(0);
        });
