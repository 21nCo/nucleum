info:
  name: Health
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/health"
runtime:
  scripts:
    - type: tests
      code: |
        test("service is healthy", function() {
          expect(res.status).to.equal(200);
          expect(res.body.ok).to.equal(true);
        });
