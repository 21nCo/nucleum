info:
  name: Widget Token Session After Revoke
  type: http
  seq: 17
http:
  method: GET
  url: "{{baseUrl}}/auth/widget-token/session"
  headers:
    - name: authorization
      value: "Bearer {{widgetToken}}"
settings:
  expectedStatus: 401
runtime:
  scripts:
    - type: tests
      code: |
        test("revoked widget token is rejected", function() {
          expect(res.status).to.equal(401);
        });
